#include <stdio.h>
#include <stdlib.h>
int main(void)
{
 int x,cont,sum,i,tabla[25];

 i=1;
 sum=1;
 for (x=1;x<=25;x++)
 {
 cont=1;
 if (x%2==1)
 {
 tabla[i]=x;
 i++;
 }
 }

 for (x=1;x<i;x++)
 {
 sum=sum+tabla[x];
 }

 printf("%d\n",sum);

 system("PAUSE");
 return 0;
}A
